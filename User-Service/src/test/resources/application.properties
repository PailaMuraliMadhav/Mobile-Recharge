# Use a dedicated test database or your local one
spring.datasource.url=jdbc:mysql://localhost:3306/omni_user?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=Murali#13

# Ensure Hibernate can create/update tables for tests
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Mocking JWT for tests
jwt.secret=TestSecretKeyForJUnitTestingPurposesOnly123456
jwt.expiration=3600000

# Disable the Config Server check for tests
spring.cloud.config.enabled=false
spring.cloud.config.import-check.enabled=false

# Provide a dummy name so the context can start
spring.application.name=user-service