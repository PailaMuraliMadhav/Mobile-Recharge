# Test Configuration for UserService
# Mail Configuration (Test values)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=test@gmail.com
spring.mail.password=testpassword
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Database Configuration (MySQL - same as production for tests)
spring.datasource.url=jdbc:mysql://localhost:3306/omni_user
spring.datasource.username=root
spring.datasource.password=${DB_PASSWORD:Murali#13}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# JWT Configuration (Test values)
jwt.secret=testSecretKeyForJwtTokenGenerationInTestEnvironment
jwt.expiration=86400000

# Logging
logging.level.com.example.userservice=DEBUG
logging.level.org.springframework.mail=DEBUG